# Discharge of the trace-73 coefficient comparison C

Date: 2026-09-02

Current binding note: the regenerated v2 witness is bound to
`audit/t73_p0_johnson_certificate.json`, not to the retired symbolic AR
witness.  C is discharged by the actual maps and state family written as
equations (17) and (24)--(27) in the paper.  S is discharged separately by
the relative move table and the genus-zero calculation (30)--(32).

## Verdict

C is **DISCHARGED** in the exact strength required by the paper:

1. the actual P0 cut coefficient admits a homogeneous quantum-trace map to
   the BPW/BHPW endpoint model;
2. the divided cubic coefficient, not the whole formal power series, descends
   through every MWW beta and psi relation;
3. the selected class has nonzero value in quantum degree 494;
4. the divided row is symmetric monoidal for cobordisms disjoint from the
   detector ball, which is the input used by relative S.

No claim is made that the complete series `D_h` factors through the W2 core
attachment.  The endpoint cap and the four two-handle core disks have
different boundaries.  Only the leading divided functional is asserted and
proved.

The finite candidate data are recorded in
`audit/t73_c_comparison_witness.json` and regenerated by
`scripts/generate_t73_c_comparison_witness.py`.

## 1. The actual coefficient bimodule

Let `C` be the pregraded rational tangle category whose objects are tangles

```text
T : P86 -> P88.
```

For the P0 cut link `R`, MWW's one-handle theorem uses the coefficient
bimodule

```text
M_R(T,T') = KhR_2(R union T' union mirror(T))
```

with the standard MWW grading shift and left/right gluing actions.

The reduced `m_2` word has 42 `y/Y` events and 269 `z/Z` events.  The
`r_xy` word has two of each.  In the cyclic word of each owner, every `y/Y`
event is followed immediately by a distinct `z/Z` event.  The witness lists
all 44 pairs.  The corresponding one-edge intervals on the actual P0 product
annuli are pairwise-disjoint subrectangles.  Removing them leaves 227
unpaired z passages, each of which closes between the two parallel annulus
boundaries to a separate product circle.

Let `F` be the framed west-to-east tangle formed by the 44 rectangles.  The
opposite boundary of the same annuli is its pivotal dual.  The fixed product
isotopy gives, for every pair `T,T'`, a graded isomorphism

```text
H_{T,T'} : M_R(T,T')
  ~= Hom_C(F T,F T'){-44} tensor A^tensor227,
A = Q[X]/(X^2).
```

This is an isomorphism of coefficient bimodules.  To check the left square,
glue a morphism `f:S->T` before the source link and then perform the fixed
product isotopy.  The resulting cobordism is the same, up to isotopy rel
boundary, as first performing the product isotopy and precomposing by `F(f)`.
The right square is the identical argument with postcomposition.  The two
equalities are associativity of gluing for one fixed simultaneous surface;
no comparison of unrelated diagrams is used.

The link-homology ingredients are pivotal tangle duality and the disjoint
union Kunneth isomorphism over `Q`.  Applying the 227 counits gives a
homogeneous coefficient-bimodule morphism because the circle factors are
disjoint from both actions.  Finally, `(T,T')->Hom(FT,FT')` is a transported
representable coefficient.  Its ordinary coefficient-HH0 reduction to the
regular trace is proved in
`Smooth4PC/RepresentableCoefficient.lean`.

## 2. Quantum trace and endpoint functor

Let

```text
R_q = Q[q,q^-1].
```

Pregrading replaces the ordinary cyclic relation by

```text
L_f(m) = q^degree(f) R_f(m).
```

Since `H` and the circle counits are homogeneous coefficient morphisms, they
carry every displayed relation to the corresponding relation in the regular
quantum vertical trace.  BPW's canonical vertical-to-horizontal trace functor
then gives a map into the quantum horizontal trace.  The strict BHPW
tangle/foam functor and its flat-base qHH0/Chern identification give the
endpoint map

```text
Sh_q : qTr_q(C;M_R) -> Hom(E86,E88),
E86=(V^tensor86)_86, E88=(V^tensor88)_86.
```

Here `E86` has rank one and `E88` has rank 88.  This construction is a
composite of functors; hence it is natural for tangle cobordisms and symmetric
monoidal for disjoint cobordisms.

Base change uses

```text
q -> 1+h,  R_q -> Q[[h]].
```

It factors through rational localization and the completion of a Noetherian
local ring, so it is flat.  In particular, the BHPW flat-base theorem applies.
For the trace cokernel itself, right exactness of tensor product already
shows that reduction modulo `h` replaces `q^degree(f)` by one and gives the
ordinary MWW coefficient `HH0`.

## 3. The selected Hattori class

Let `U:P86->P88` be the selected one-cup tangle in the P0 endpoint order and
put

```text
T_1 = F^-1 U.
```

Define

```text
v_T = H^{-1}(Id_U tensor X^tensor227).
```

Its quantum-trace class is represented before any quotient by a named
diagonal coefficient element.  The circle counits evaluate it to the cup, so

```text
Sh_h([v_T]) = u_h.
```

The degree ledger is

```text
Hom shift -44 + 227 X-labels + one-handle shift 315
  + cabled shift (-4) = 494.
```

## 4. Endpoint coordinates and convention robustness

BPW's evaluation/coevaluation formulas show that, after restricting to the
one-defect weight spaces and applying the position-dependent basis used for
the Burau block, the constant cup and cap terms have the form

```text
u_0   = e_0 +/- e_5,
ell_0 = e_87^* +/- e_2^*.
```

All omitted coefficients are invertible q-monomials and higher h-corrections.
The normalized checked R-matrix on two adjacent one-defect basis vectors is

```text
[[1-q^-2,q^-1],[q^-1,0]].
```

After the basis change `diag(1,q^-1)` and `t=q^-2`, it is the public positive
unreduced Burau block; the negative crossing is its inverse.

For mixed orientations, BPW's oriented-cabling formula differs from the
all-positive model by a writhe-dependent grading shift and pivotal basis
maps.  The complete public word has writhe zero, so the global shift vanishes.
Any remaining constant permutation or pivotal chart is applied
simultaneously to `W,u,ell`; the transported matrix coefficient is exactly
unchanged.  Public endpoint labels are chosen after this common transport.

The point-push word begins in order three on the entire endpoint module.  It
follows from the cubic-jet lemma that higher h-corrections in the cup, cap and
basis changes do not affect the cubic coefficient.  The witness evaluates
all four possible constant sign choices:

```text
cup +, cap + : -53824
cup +, cap - : -53760
cup -, cap + : -59008
cup -, cap - : -59072
```

Every value is nonzero.  The last line is the public normalization.  Thus the
argument does not depend on an unresolved pivotal sign.

## 5. Divided one-handle detector

Over `Q[[h]]` define

```text
D_h = cap_h (rho_h(W)-I) Sh_h.
```

This is a well-defined map on the quantum coefficient trace: `Sh_h` already
kills every quantum cyclic relation, and the remaining factors are fixed
linear postcompositions of its one endpoint Hom-space.

The exact Magnus/Andreadakis certificate and the full 88-dimensional check
give

```text
rho_h(W)-I in h^3 End(E88)
```

on all 7,744 matrix entries.  Hence `D_h` takes values in `h^3 Q[[h]]`.
Since this ring is a domain, division by `h^3` is unique.  Reducing the divided
map modulo `h` defines

```text
D_3 : HH0(C;M_R) -> Q.
```

Lift independence follows because replacing a lift by `x+h y` changes the
divided value by a multiple of `h`.

On the selected class,

```text
D_3([v_T]) = -59072
```

in the public convention, and is nonzero in every strict pivotal convention
listed above.

## 6. Passage through the MWW two-handle quotient

Only the divided cubic row is claimed to descend.  This is enough for
nonvanishing in the ordinary rational lasagna module.

### Beta relations

At every cable state, apply the P0 collar isotopy simultaneously to every
physical copy.  A sign-preserving beta braid changes a chosen placement of
the one-cup detector to another placement.  Average the row over the finite
set of such placements.  Constant permutations merely reindex this average.
After standardizing a placement, the remaining pure braid acts as `I+O(h)`.
Conjugating `rho(W)-I=O(h^3)` and transporting the cup and cap simultaneously
therefore changes the pairing only in order four.  Thus the cubic average is
exactly beta invariant.

This is the algebra proved by `TransportedPairing.lean`, `CubicJet.lean` and
the normalized-orbit lemmas in `AugmentationCocone.lean`.  It does not assume
that anti-parallel braid actions factor through a symmetric group.

### Undotted psi relations

Filter the endpoint Temperley--Lieb category by through degree.  Composition
cannot increase through degree.  The selected cross-owner one-cup cell has
through degree 86.  For every owner meeting the detector gate, undotted
balanced pair creation introduces at least two cups and its complete
action-closed ideal has through degree at most 84.  Its image is therefore
zero in the degree-86 associated graded used by the detector.

The only zero-gate owner is the transported `r_zx` component.  Its local pair
factor is handled directly by the two core counits:

```text
(epsilon tensor epsilon) Delta(1)=0.
```

Hence every undotted psi image is killed on the whole source.

### Once-dotted psi relations

For a dotted pair addition, close the two new copies by the same two-handle
core counits used in MWW's map.  The local identity

```text
(epsilon tensor epsilon) Delta(X)=1
```

pulls the target row back to the source row.  The raw degree `+2` is cancelled
by the target cabled shift `-2`.  Beta invariance makes the statement
independent of which physical pair is chosen.  The normalized placement
ratios telescope along successive additions, so the equation holds at every
cable level.

These three cases prove that the family of divided rows annihilates the full
linear span of MWW's beta, undotted-psi and dotted-psi-minus-identity
relations.  The quotient universal property and MWW Theorem 3.2 give a
functional on the actual W2 lasagna module whose selected value is nonzero.

## 7. Monoidality needed by S

The detector point-push and cap are supported in the P0 ball.  A cobordism
disjoint from that ball is carried by the symmetric-monoidal endpoint functor
to a tensor factor independent of `rho(W)-I`.  This is the naturality input
used in relative S.  It does not by itself evaluate an essential
`S^2 x {0}` sphere: those six dotted/undotted endpoint values are the
remaining equations (31).

## 8. Independent replay

Run:

```text
python -B scripts/generate_t73_c_comparison_witness.py --check
python -I -B tests/test_t73_c_comparison_witness.py -v
```

The current witness SHA-256 is

```text
6B574D3E062EE5D41D5D0F74C5C85CFC4BBFA773D335F00DF8107232EA1E1573.
```

The replay checks every y-to-z product pairing, the 227 residual circles, the
P0 dependency, the public braid identity, all four endpoint sign conventions,
the selected value, and hostile pairing mutations.  The categorical steps are
the explicit proof above using the cited primary functors.

## Conclusion

The candidate comparison C is constructed through the two-handle stage.
Combined with P0, it gives a nonzero rational `N=2` class there in quantum
degree 494.  Section 8 proves its passage through the three handles.
