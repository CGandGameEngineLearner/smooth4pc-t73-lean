# P0 discharge by the public Aitchison--Rubinstein product construction

Date: 2026-09-02

## Verdict

P0 is **DISCHARGED for the replacement presentation used by the paper**.
The historical two-million-crossing PD is neither recovered nor identified;
it is retired from the proof.

The replacement is defined directly from the actual Aitchison--Rubinstein
handle construction, transported to the displayed matrix, simplified by two
standard geometric cancellations, and equipped with a standard embedded
detector collar.  The construction is recorded in
`audit/t73_ar_product_witness.json` and regenerated by
`scripts/generate_t73_ar_product_witness.py`.

## 1. Public primary source

Use I. R. Aitchison and J. H. Rubinstein, *Fibered knots and involutions on
homotopy spheres*, Contemporary Mathematics 35 (1984), 1--74.  A complete
public scan is hosted by
[Robion Kirby's Berkeley archive](https://math.berkeley.edu/~kirby/papers/Gordon%20and%20Kirby%20%28editors%29%20-%20Four-manifold%20theory%20%28Durham%29%20-%20MR0780574.pdf).
The 519-page PDF is unencrypted and has SHA-256

```text
6F7E95B8266876774667AD40EA3DE964B165680D6789A34E49BF598C3AE04DF0.
```

The load-bearing passages are:

- pp. 5--7: simultaneous mapping-torus handles, their core circles, framing
  annuli, and the complementary base 1/2-handle cancellation;
- pp. 8--12: the coordinate-spine Heegaard model of `T^3`, the standard dual
  2-handles, and the isotopy to a handlebody-preserving representative;
- pp. 16--17: product-framing convention and the parallel-strip construction
  in the universal cover.

## 2. Matrix and coordinate change

Let

```text
A = [[0,269,1240],[0,41,189],[1,0,32]].
```

The witness also records

```text
C_AR = [[0,0,1],[713,83,0],[-902,-105,-10]],
R    = [[71,-466,1],[-610,4004,-7],[1,-7,-2]].
```

Direct integer arithmetic gives

```text
det(R)=1,
C_AR R = R A,
det(A)=det(A-I)=det(C_AR)=det(C_AR-I)=1.
```

Thus the coordinate change induced by `R` conjugates the two torus
automorphisms.  Transporting the complete AR handle construction through this
fiber diffeomorphism preserves all embeddings, labels and normal fields.
Because `det(R)=1`, its derivative lies in the connected group
`GL^+(3,R)` and preserves the homotopy class of the canonical normal framing
of the section circle, including the untwisted `epsilon=0` choice.  One may
equivalently apply the general AR construction directly to `A`.

## 3. The actual simultaneous framed link

Use the coordinate-spine genus-three Heegaard splitting of `T^3`.  Let
`C_i` be the three oriented coordinate spine circles based at the common
0-spine `Q`, and let `A_i` be pairwise-disjoint narrow disk neighborhoods of
their portions away from `Q`.

Let `phi_A` be the linear torus automorphism, straightened to the identity in
a section ball.  AR Lemma 3.1 gives an isotopic handlebody-preserving
representative `psi_A`; the isotopy may be taken relative to the section ball
because its endpoints agree there.

For every `i=1,2,3`, AR p. 6 constructs the mapping-torus 2-handle with core

```text
m_i = C_i^- union lambda_i union psi_A(C_i)^+ union mu_i.
```

Here `lambda_i` and `mu_i` are the two arcs through the base 1-handle.  Its
framing annulus is constructed simultaneously as

```text
A_i^- union (lambda_i times I) union psi_A(A_i)^+
       union (mu_i times I).
```

The disks, cones, and normal levels in the AR construction are chosen
pairwise disjoint, so these are simultaneous embedded framed components, not
separate word representatives.

The three additional fiber 2-handles are the standard dual 2-cells of the
coordinate-spine Heegaard splitting.  With the chosen orientations their
attaching words are

```text
r_xy=[x,y],  r_yz=[y,z],  r_zx=[z,x].
```

Their embedding is the one supplied by the dual 2-cells, and their framing is
the product zero framing.  No inference from the words is used to define
these components.

Finally, the untwisted section-surgery handle `h_CS` goes geometrically once
over the base 1-handle `t`.  Altogether this gives one 0-handle, four
1-handles, seven 2-handles, three 3-handles and one 4-handle.

## 4. Returning from `psi_A` to the linear map

Let `rho_u`, `u in [-1,1]`, be an isotopy from `phi_A` to `psi_A`, stationary
near both endpoints and relative to the section ball.  Put

```text
g_u = rho_u phi_A^{-1}.
```

For the mapping-torus convention `(x,-1)~(phi(x),1)`, define

```text
F([x,u]_{phi_A}) = [g_u(x),u]_{psi_A}.
```

This is well defined because

```text
psi_A g_{-1} = g_1 phi_A;
```

indeed `g_{-1}=id` and `g_1=psi_A phi_A^{-1}`.  Endpoint stationarity makes
`F` smooth across the quotient seam, and the family `g_u^{-1}` gives its
inverse.  Since the isotopy is relative to the section ball, `F` fixes the
base handle and surgery pair there.  Pulling the complete AR handle
decomposition back by `F` replaces every top core by the linear image
`phi_A(C_i)` while transporting the complete framing annulus and every other
component simultaneously.

## 5. Exact word projection and product framing

Lift `C_i` and `phi_A(C_i)` to the universal cover.  The top centerlines begin
at the common point `A(Q)`.  Translate the entire top fiber simultaneously by
`-A(Q)` and extend that translation over a collar of the top fiber.  This is
isotopic to the identity, transports the whole link and normal fields
together, and changes no attaching manifold.  Since the core circles are
unbased, choosing a different first face crossing only cyclically permutes the
recorded word.  We may therefore use the segment from `0` to `A e_i`.  Its
crossings with the integer
coordinate faces occur at the rational times

```text
k/(A e_i)_j.
```

A fixed arbitrarily small product perturbation resolves simultaneous face
events.  Reading the base-handle arcs and the oppositely oriented bottom core
therefore gives exactly

```text
m_i = t phi_A(x_i) t^-1 x_i^-1.
```

This is the rule implemented by
`generate_t73_compact_kirby_ledger.py`.  The verifier regenerates the full
ordered words, not merely their exponent sums.

For the normal field, AR pp. 16--17 push the three coordinate axes in the
universal cover in direction `(1,1,1)`.  The resulting strips have parallel
boundary components; linearity of `A` preserves parallelism on their images.
After projection they are the actual framing annuli.  The `lambda/mu`
rectangles carry the product normal across the base handle.  Hence the
framing transported by every step is an explicit normal field, not a named
blackboard integer.

## 6. First cancellation

AR p. 7 identifies `(t,h_CS)` as a complementary geometric 1/2-handle pair.
Slide each passage of every other 2-handle over `h_CS` along its displayed
product rectangle, thereby removing all `t` and `t^-1` passages, and cancel
the pair.  This is the standard simultaneous cancellation algorithm stated
on that page.  The chosen untwisted surgery framing and product rectangles
give zero relative twist.

## 7. Second cancellation

The first column of `A` is `e_3`.  Thus, after the first cancellation,

```text
m_1 = z x^-1.
```

The actual core has one geometric passage over the `x` 1-handle, so `(x,m_1)`
is a complementary 1/2-handle pair.  Slide every other `x` passage over
`m_1` using parallel product bands, replace `x` by `z`, and cancel.  The whole
link, owner/cocore labels and normal fields are transported simultaneously.

The resulting handle counts are

```text
h0=1, h1=2, h2=5, h3=3, h4=1.
```

The public replay gives the exact surviving `m_2,m_3,r_xy,r_yz` words and the
actual transported image of `r_zx`.  The free word of the last component is
empty, but no split-unknot conclusion is used or asserted.

## 8. Embedded detector collar

Cut the remaining genus-two 1-handlebody along the `y,z` belt disks.  In the
selected `m_2/r_xy` cable state, the actual attaching curves have 42 `y`
passages from `m_2` and two from `r_xy`.  After choosing distinct product
normal levels, a regular neighborhood of these 44 local passages is a
standard 3-ball containing a punctured-disk collar `D^2 times I`.

The six-sweep schema defines an element of the pure braid group on these 44
labelled punctures.  The mapping-class interpretation of the braid group
realizes it by an isotopy of the punctured disk, and isotopy extension gives
an ambient isotopy of the 3-ball.  Choose the terminal extension to be the
identity to first order near every returned puncture.  It therefore returns
every wicket and each product normal, and acts on every oriented parallel
cable copy simultaneously.  The zero writhe is an additional consistency
check, not the framing proof.

The public verifier obtains

```text
B44 length  = 11340,
B44 SHA-256 = 7C2D2F792C2672221A76CAF08A71F560AF0CB7654B4D537C00FAD00B16EFA187,
permutation = identity,
writhe      = 0.
```

This is a newly defined actual collar inside the AR presentation.  No claim
that it is the collar of the retired historical PD is needed.

## 9. Independent replay

Run:

```text
python -B scripts/generate_t73_ar_product_witness.py --check \
  --source-pdf PATH_TO_PUBLIC_AR_SCAN
python -B scripts/check_t73_p0_embedded_witness.py \
  audit/t73_ar_product_witness.json
python -I -B tests/test_t73_ar_product_witness.py -v
```

The current witness digest is

```text
AA9328EA3C7A53B937C97A02D611850D0E8EEAF87C68756C04B013A486F6F112.
```

The replay checks the source PDF digest, the unimodular matrix bridge, both CS
determinant conditions, the complete word/cancellation ledger, the 44-strand
collar and hostile local-knotting mutation.  The mathematical identification
of the generated smooth handle charts with the CS construction is the
argument above, based directly on the cited AR construction.

## Conclusion

The replacement framed presentation and detector collar are actual objects
in the standard Cappell--Shaneson manifold `Sigma_A^0`.  P0 is therefore
discharged without the unavailable historical PD.  The remaining task is C:
identifying the actual cut coefficient bimodule of this product presentation
with the completed MWW/BPW endpoint model.
