# Theorem C: coefficient-trace comparison

**Status:** `DISCHARGED`

The complete proof is Section 7 of the LaTeX paper.  Its public finite witness
is `audit/t73_c_comparison_witness.json`, regenerated by
`scripts/generate_t73_c_comparison_witness.py`.

## Load-bearing construction

1. The P0 product annuli contain 44 disjoint one-edge y-to-z
   subrectangles; 227 z passages remain as separate product circles.
2. The fixed product isotopy and pivotal duality give a two-sided coefficient
   bimodule equivalence

   ```text
   M_R(T,T') ~= Hom(F T,F T'){-44} tensor A^tensor227.
   ```

3. Circle counits and the representable-coefficient theorem reduce this to
   the regular quantum vertical trace.  BPW/BHPW give the strict completed
   endpoint map.
4. The selected Hattori identity maps to the one-cup vector in degree 494.
   The public normalized cubic is `-59072`; all four pivotal sign choices
   are independently nonzero.
5. Only the divided cubic is descended through W2.  Beta permutations are
   handled by normalized transported placement orbits, pure residuals start
   one order later, undotted psi images lie below through degree 86 or are
   killed by the core counit, and dotted psi uses
   `(epsilon tensor epsilon) Delta(X)=1`.
6. The resulting W2 row is symmetric monoidal outside the detector ball,
   supplying the input used by relative S.

## Replay

```text
python -B scripts/generate_t73_c_comparison_witness.py --check
python -I -B tests/test_t73_c_comparison_witness.py -v
```

Witness SHA-256:

```text
410B1C411F321DD83F8ACD76CF5C259DF2EE341629B13669C2FE458005E5D922
```

The older full-series W2 factorization remains false and is not used.  The
endpoint cap is not identified with the four W2 core disks; the proved object
is the leading divided cocone.
