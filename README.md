# Smooth4PC T73 Lean Audit

This repository is a kernel audit of the `X(41,189,73)` falsification chain.

The current target is `CONDITIONAL_KERNEL_CLOSED`. The project may claim an
unconditional counterexample only after every mathematical interface has a
Lean proof and both `conditionalIsHomotopySphere` and
`conditionalNotStandard` are combined into an axiom-audited theorem with no
external hypotheses.

The authoritative Git repository lives on the local Windows host. The remote
server is a non-Git build sandbox only.
