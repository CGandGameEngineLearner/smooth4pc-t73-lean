# hardened TH1 chosen component：敌审终裁

日期：2026-08-31（Asia/Tokyo）  
geometry receipt SHA：
`EE620E6B085A5F9E1C73CFDD1AD04FC0682CEC74DA3DBF8AFE70DD19C038E3A0`

## 判词

```text
350176 leaves/fingers + 350175 bands exhaustive: PASS
independent geometry predicates:                 PASS
material/root ancestry:                          PASS
launch/final semantic transform identity:         PASS

NEW CHOSEN TH1 embedded component:                PASS
surface -> constant BHPW map binding:             PASS at h3
current cap scalar pair:                           0 / 0
alternative cap scalar pair:                       0 / 0
historical TH1 claim:                               NO / not needed
global HJ triple:                                  waits THXY + joint gate
```

## 核心复核

receipt计数：

```text
band/finger self intersections: 0/0;
band mutual projection crossings: 1;
band old projection crossings: 0;
finger old projection crossings: 491,777,890.
```

finger crossings的3D避免与TH2同理：forward/owner/inverse exact heights为
`3/16,8/16,13/16`，old diagram在base layer；不同leaves又占严格不交slabs与
injective D2 indices。band唯一mutual crossing用 `2/8` 与 `6/8` 分层，
`4/8` branch core只在指定位置连接。

九项hardened mutations全部通过。root cap逐项消费last geometry/SLP/terminal
hash，657,041,664-letter stream free-reduces为空。

### launch/final diff

TH1 decision记录launch runner SHA与final runner SHA不同。直接加载final runner
但不重跑350k行，重新生成dynamic transformed module SHA，结果与receipt逐字
相同：

```text
b0820a46fd5c6e99025eadf0bc68fc724040561acd7b8555168e0978c6be933f
```

所以final改动只在独立finger predicate wrapper/receipt层，未改变实际geometry
producer acceptance set；该账关闭。

## H0与scalar

全部noninvertible bands/cap属于new-only TH1 tree，old P86/P88 block沿identity
cylinders。mixed isotopy/braid在constant endpoint qHH为identity，正阶最早h4。
因此h3 map为 `Id_old tensor T_new`。

W2 core counits是同一chosen surface的actual target caps；new tree的undotted
column含唯一1、dotted column全X，给 `E(U)=0,E(D)=1`。故两套incoming cap
detectors均得scalar `0/0`。

TH1这一腿可以升为chosen HJ component PASS；不认领historical movie。

