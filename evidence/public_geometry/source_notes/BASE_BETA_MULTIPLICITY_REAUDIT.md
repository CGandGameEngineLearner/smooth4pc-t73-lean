# Base beta multiplicity hostile re-audit

Date: 2026-08-30

## Verdict

```text
FINAL_BASE_SCALAR = -59072
```

`-28864` is invalid: it Reynolds-averaged gate passages as though they were
independent physical cable copies.

## Exact base node

The actual t73 cut object fixes

```text
alpha = (0,0,0,0,0),
r     = e_m2 + e_rxy.
```

MWW's formula is

```text
k_i^- = r_i-alpha_i^-,
k_i^+ = r_i+alpha_i^+.
```

Therefore the only active owners have

```text
rxy: k^- = 1, k^+ = 1,
m2:  k^- = 1, k^+ = 1.
```

The serialized physical-component ledger independently contains exactly four
components: one negative and one positive rxy cable, and one negative and one
positive m2 cable.  Their y-gate passage counts are respectively `2` and
`42` **per physical copy**.  Passage count is not `k_i^+` or `k_i^-`.

## Consequence for beta

For each active owner, MWW's beta group is `B_(1,1)`.  Its constant
permutation quotient is trivial because both colour sets are singletons.  It
is the pure two-strand braid group, generated by `sigma_1^2`.  In the endpoint
representation

```text
rho_h(sigma_1^2)=I+O(h).
```

Hence every beta correction multiplies the already cubic
`rho_h(W)-I=O(h^3)` and begins at order four.  No finite Reynolds average is
needed at the base node.  The raw complete-row evaluation therefore remains

```text
[h^3] ell(rho_h(W)-I)(e0-e5) = -59072.
```

The former groups

```text
S2 x S2 x S42 x S42
```

incorrectly permuted gate passages inside a single cable.  They are not the
MWW two-handle beta groups at this node, so `-28864` must be retired.

## Higher levels

After a psi move an owner has two negative and two positive physical copies,
so the constant quotient becomes `S2^- x S2^+`.  It swaps whole passage
blocks pointwise; it still does not create an internal `S_g` action on the
`g=2` or `g=42` gate positions.  Any all-level Reynolds construction must
retain the complete gate-indexed row.

Reproduce:

```powershell
python -B D:\tmp\r6\agents\c3_exact_hostile_audit\base_beta_multiplicity_reaudit.py
```
